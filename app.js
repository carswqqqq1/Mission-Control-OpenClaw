const agents = { CEO: { name: 'Arroyo CEO Agent', role: 'Executive', status: 'idle' }, Owen: { name: 'Owen', role: 'Lead Hunter', status: 'idle' }, Bob: { name: 'Bob', role: 'Website Auditor', status: 'idle' } }; let activeAgents = []; function log(m, t) { const l = document.getElementById('logs'); const e = document.createElement('div'); e.className = 'log-entry ' + (t || 'info'); e.textContent = '[' + new Date().toLocaleTimeString() + '] ' + m; l.insertBefore(e, l.firstChild); } function spawnAgent(n) { if (activeAgents.includes(n)) { log(n + ' already active', 'error'); return; } activeAgents.push(n); agents[n].status = 'active'; updateUI(); log('Spawned ' + agents[n].name, 'success'); log('Agent ' + n + ' ready', 'agent'); } function killAgent(n) { const i = activeAgents.indexOf(n); if (i > -1) { activeAgents.splice(i, 1); agents[n].status = 'idle'; updateUI(); log('Terminated ' + n, 'error'); } } function updateUI() { document.getElementById('active-agents').textContent = activeAgents.length; const list = document.getElementById('active-agents-list'); list.innerHTML = activeAgents.map(n => '<div class="agent-card active" onclick="killAgent(\'' + n + '\')"><span class="role">' + n + '</span><span class="status">' + agents[n].role + '</span></div>').join(''); } updateUI(); log('Command Center ready', 'success');
